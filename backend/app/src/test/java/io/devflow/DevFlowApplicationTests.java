package io.devflow;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Smoke test: the whole modular monolith must boot (all modules wire correctly). */
@SpringBootTest
class DevFlowApplicationTests {

    @Test
    void contextLoads() {
    }
}
