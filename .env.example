# .env.example — copy to .env and fill in values as needed.
# AI_MODEL_CHAT controls which Spring AI model starter is active.
# Set to "openai" and provide OPENAI_API_KEY to enable the LLM client.
OPENAI_API_KEY=
AI_MODEL_CHAT=none
